table:
  name: bootstrap_address_bindings
  schema: public

select_permissions:
  - permission:
      columns:
        - chain_type
        - source_addr
        - target_addr
        - created_at
        - updated_at
      filter: {}
      limit: 1000
    role: anonymous

