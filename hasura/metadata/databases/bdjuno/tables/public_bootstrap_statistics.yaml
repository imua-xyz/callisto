table:
  name: bootstrap_statistics
  schema: public

select_permissions:
  - permission:
      columns:
        - one_row_id
        - tvl
        - updated_at
      filter: {}
      limit: 1
    role: anonymous
